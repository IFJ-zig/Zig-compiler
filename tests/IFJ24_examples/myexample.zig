// Program 2: Vypocet faktorialu (rekurzivne)
const ifj = @import("ifj24.zig");
// Hlavni funkce
pub fn test_fn(n: i32, m: i32) void {}
pub fn main() void {
    var x = (2+2)*2;
    x = x / 8;
    if(2-x) {}

    if(x) |y| {}

    if(x) |y| {}else {}

    while(2*(x+6)/2){}

    test_fn(x,2);

    x = test_fn(2, 9)+2;
}
